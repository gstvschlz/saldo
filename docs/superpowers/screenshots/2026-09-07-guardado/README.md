# guardado — captura

Emulador `emulator-5554`, tema claro, build debug instalado do zero (`pm clear` + onboarding
com saldo inicial de R$ 1.000,00) e duas movimentações lançadas à mão: `salario` (entrada,
+R$ 5.000,00) e `cdb` (saída, natureza economia, −R$ 1.000,00).

| arquivo | o quê |
|---|---|
| `01-hero-guardou.png` | o hero do board (`saldos`), com a pill "guardou 20%" ao lado de "no mês" |

Nesta captura a pill "guardou 20%" saiu mais alta que a "no mês": era o alvo de toque de 44 dp pintado. O commit seguinte moveu o alvo para fora da pill, que ficou do mesmo tamanho da vizinha.
