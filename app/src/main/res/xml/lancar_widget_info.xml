<?xml version="1.0" encoding="utf-8"?>
<!--
    2x1: dois botoes, so isso. Nao cresce em altura porque nao ha o que mostrar a mais —
    uma celula mais alta so acrescentaria padding.
-->
<!--
    `previewLayout` só existe no Android 12+, e é isso mesmo: a decisão foi prévia em XML,
    sem `previewImage` de vetor para as versões antigas. Abaixo do 12 o seletor mostra o
    ícone, como sempre mostrou.
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="UnusedAttribute"
    android:minWidth="110dp"
    android:minHeight="40dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:maxResizeWidth="400dp"
    android:maxResizeHeight="120dp"
    android:targetCellWidth="2"
    android:targetCellHeight="1"
    android:resizeMode="horizontal|vertical"
    android:updatePeriodMillis="21600000"
    android:widgetCategory="home_screen"
    android:description="@string/widget_lancar_descricao"
    android:initialLayout="@layout/glance_default_loading_layout"
    android:previewLayout="@layout/preview_lancar" />
